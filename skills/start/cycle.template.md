# {버전} {사이클명}

| 사이클 | 버전 | 상태 | 시작 | 갱신 |
|---|---|---|---|---|
| {사이클명} | {버전} | 진행 | {YYYY-MM-DD} | {YYYY-MM-DD} |

## 1. 범위

{한 줄. 이 사이클이 끝나면 무엇이 달라지나.}

### 1.1 {항목 이름} — #{n}

- 무엇을: {지금 상태 → 달라질 상태. 구체적으로}
- 어떻게: {어느 층을 어떤 방향으로. 접근 수준}
- 끝(SC-1): {실증 가능한 문장} — {CLAUDE.md cycle 절의 수단}
- 왜 이렇게: {이 방향인 이유. 버린 길이 있으면 왜. 1~2줄}

### 1.2 {항목 이름} — #{n} · #{m}

…

제외: {인접해서 손대고 싶어질 것. 없으면 이 줄을 지운다}
이월: #{n} — {선행 미해결 등 이유. 없으면 이 줄을 지운다}

## 2. 배치

| 배치 | 이름 | 항목 | 검증 | 상태 | 메모 |
|---|---|---|---|---|---|
| B-1 | {이름} | 1.1 | `{배치 범위로 좁힌 명령}` → {무엇을 보면 끝} | 미착수 | {파일 · 접근 한 줄} |
| B-2 | {이름} | 1.1 · 1.2 | 육안 {무엇을} | 미착수 | |

<!-- 상태: 미착수 · 진행 중 · 대기(육안) · 완료 · 미실증. 완료면 검증 칸에 결과 수치를 덧붙인다. 커밋은 `B-n: {이름}` -->

## 3. 결정

<!-- 현재값만. 되돌리면 행을 덮어쓴다. 미실증은 `SC-n 미실증 · 재현 불가 | 검증 안 함` — 종류는 사용자가 정한 것만. 범위 밖 발견은 `발견: {무엇}` -->

| 배치 | 정한 것 | 이유 |
|---|---|---|

## 4. 결과

<!-- close가 쓴다 -->

| SC | 판정 | 근거 |
|---|---|---|
| SC-1 | {됐다 | 미실증(재현 불가) | 미실증(검증 안 함)} | {B-n 검증 · 최종 검증} |

- 최종 검증: `{명령}` → {결과}
- 다음에 바꿀 것: {실제로 바꿀 것. 3줄 이내}

| 발견 | 어디서 | 비고 |
|---|---|---|
| {범위 밖에서 본 것 한 줄} | B-{n} | {승격은 사용자} |

| 이슈 | 처리 | 비고 |
|---|---|---|
| #{n} | 닫음 | |
| #{n} | 이월 | 남은 것: {…} |
