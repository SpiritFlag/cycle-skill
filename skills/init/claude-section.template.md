## cycle

<!-- init이 실측해 세운 이 프로젝트의 규칙. 스킬 절차는 여기 쓰지 않는다. 기본값과 같은 줄은 지운다. -->

- 검증: `{명령}` ({무엇을 · 소요}) · `{느린 명령}` ({외부 자원 등}) · 육안 확인은 사용자
- 브랜치: `{통합}` → `{릴리즈}` ({ff-only | PR merge commit})
- 종료 훅: {close가 추가로 하는 일. 예: `package.json` version}
- 릴리즈 노트: 제품명 {…}, {톤}
